# 함수, function
# 정의하다. define
# 함수명은 스네이크 케이스

def func_hello():
    pass

# 인수, 반환값
# 호출 == 사용
a = print("hello", "python") # print는 인수는 있는데 반환값(return 값)이 없는 함수
print(a)

# 1. 인수 없음, 반환값 없음
# 2. 인수 없음, 반환값 있음
# 3. 인구 있음, 반환값 없음
# 4. 인수 있음, 반환값 있음
